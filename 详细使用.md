# LeekShell C2 Server —— 详细使用文档

> 适用范围：单文件构建的 **Windows `c2_server.exe`** 与 **Linux ELF `c2_server`**（同一套 Python 逻辑，用法完全一致）。
> 源码运行（`python c2_server.py`）亦适用，仅配置定位方式有差异（见 §5.2）。

---

## 1. 快速开始

### 1.1 首次运行

两种二进制都要求机器装有对应 OS（exe→Windows x64，ELF→Linux x86_64），无需 Python 运行时（已打包）。

```bash
# Windows
c2_server.exe                        # 默认找 exe 同目录的 config.yaml

# Linux（建议常驻方式）
cd /opt/lks && setsid nohup ./c2_server > server.log 2>&1 < /dev/null &
```

**首次运行时若找不到 config.yaml**：程序会在**自身所在目录**自动生成一份默认配置（`secret_key` 留空→随机生成并写回该文件；AI 默认关闭；默认仅启动 TCP 4444）。

> 打包体**不包含**任何敏感配置（不打包 config.yaml / 密钥 / 沙箱采集数据）。运行所需配置全部来自 exe/ELF **同目录的 config.yaml**（或 `-c` 指定路径），这是刻意设计：换一台机器跑就是一份全新干净配置。

### 1.2 默认端口与登录

| 服务 | 默认 | 说明 |
|---|---|---|
| Web 控制台 | `http://<host>:4433` | 双层登录 |
| TCP C2 | `4444` | TCP Agent 监听 |
| WSS C2 | `8443` | WSS Agent 监听（需证书） |

双层登录（防指纹 / 混淆入口）：
1. **第一层预认证**（伪装企业 SSO 页）：`operator` / `changeme`
2. **第二层控制台登录**：`admin` / `leekshell2026`

> 以上为内置默认口令；生产使用**必须**改（config `auth.*` 或环境变量/CLI 覆盖）。

---

## 2. 配置文件 config.yaml 详解

路径：默认取**可执行文件同目录**的 `config.yaml`；`-c /path/config.yaml` 可指定任意路径。配置优先级见 §4。

> 提示：**单文件首跑自动生成的默认配置**以本文为准（web 4433 / TCP 4444 / WSS 关、AI 关）。若改用**仓库附带的 config.yaml**（开发配置，web 8445、WSS 8443、AI 开/指 anthropic 网关），则以其文件内容为准。

### 2.1 完整注释示例

```yaml
# ── Web 控制台 ───────────────────────────────────────────────────────────
web:
  host: "0.0.0.0"        # 监听地址
  port: 4433             # 控制台端口
  debug: false           # true 时开启结构化调试日志(stderr)（等价 --debug）
  https: false           # true 则控制台走 HTTPS（证书复用 wss_listener 的 cert/key）

# ── 会话签名密钥 ─────────────────────────────────────────────────────────
server:
  # 留空 "" → 首次运行自动生成 64 位 hex 并写回本文件；请勿硬编码固定值
  secret_key: ""

# ── 双层登录 ─────────────────────────────────────────────────────────────
auth:
  preauth_user: "operator"     # 第 1 层用户名
  preauth_pass: "changeme"     # 第 1 层口令
  login_user:   "admin"        # 第 2 层用户名
  login_pass:   "leekshell2026" # 第 2 层口令

# Payload Generator 默认回填的服务器地址（生成 exe 载荷时的 Host 预填）
server_host: "127.0.0.1"

# ── TCP C2 监听 ──────────────────────────────────────────────────────────
tcp_listener:
  port: 4444

# ── WSS C2 监听（TLS）────────────────────────────────────────────────────
wss_listener:
  port: 0            # 0=关闭该监听；>0 表示 WebSocket over TLS 端口
  cert: "templates/leekshell.crt"   # TLS 证书路径（相对 config 目录/内置资源）
  key:  "templates/leekshell.key"   # TLS 私钥路径
  auto_cert: false   # true 且文件缺失时，自动生成自签名证书到上述路径

# 自动启动的监听器列表（每项一个监听）
auto_listeners:
  - { protocol: "tcp", port: 4444 }
  - { protocol: "wss", port: 8443 }

# ── 沙箱 / 访客采集（web 控制台入口的蜜罐面）─────────────────────────────
sandbox:
  enabled: true            # 访客采集开关
  trust_proxy_headers: true# 信任 X-Forwarded-For / X-Real-IP / CF-Connecting-IP
  record_failed_creds: true# 记录失败登录尝试的口令（成功登录绝不记录）
  capture_headers: true    # 每条记录保存完整请求头
  collect_fingerprint: true# 在预认证页注入被动指纹 JS
  enrich_url: ""           # 可选威胁情报接口，{ip} 会被替换成目标 IP
  enrich_token: ""         # 上述接口的 Bearer token
  persist_dir: "results/visitors"  # 持久化目录（相对 config 目录）
  max_in_memory: 500       # 控制台表格保留的最近记录条数

# ── BOF（内置编译 + 插件）────────────────────────────────────────────────
bof:
  compiler: ""             # 服务器端 MinGW gcc 绝对路径；空=自动探测
  plugins:
    enabled: true          # 插件系统开关
    directory: "plugins/bof"  # 插件目录：优先 exe 同目录同名目录，否则用打包内置示例
    max_plugins: 256
    cache_bytes: 33554432  # 编译产物缓存上限(32MB)

# ── AI Console（副驾，确认制）───────────────────────────────────────────
# 顶层 enabled/provider 也可在 Web「⚙ AI 设置」页实时改（见 §8），
# Web 改动持久化到 ai_store.json 并覆盖本文件对应项。
ai:
  enabled: false            # true 启用 AI 控制台
  provider: "ollama"        # ollama | anthropic | deepseek
  ollama:
    base_url: "http://127.0.0.1:11434"  # 仅本机回环，勿改公网
    model: "qwen3"
    timeout: 120
  anthropic:
    model: "claude-sonnet-5"
    api_key_env: "LKS_ANTHROPIC_KEY"    # 原生 x-api-key（环境变量名）
    auth_token_env: "ANTHROPIC_AUTH_TOKEN"  # 兼容网关 Bearer（环境变量名）
    base_url: "https://api.anthropic.com"   # 可指向 Anthropic 兼容网关
    timeout: 120
  deepseek:
    base_url: "https://api.deepseek.com"
    model: "deepseek-chat"
    api_key_env: "LKS_DEEPSEEK_KEY"
    timeout: 120
  max_compile_iterations: 3  # AI 写 BOF 单请求编译迭代上限
  approval_ttl: 30           # 一次性批准态有效期（秒）
  session_max_messages: 200  # 每 agent 会话消息上限
```

### 2.2 各字段取值/默认速查

| 键 | 默认 | 可选值 / 说明 |
|---|---|---|
| `web.host` | `0.0.0.0` | IP / 主机名 |
| `web.port` | `4433` | 1–65535 |
| `web.debug` | `false` | 结构化调试日志 |
| `web.https` | `false` | 复用 WSS 证书；置 true 需证书可用 |
| `server.secret_key` | `""`(自动生成) | 64-hex 会话签名密钥，勿手写固定值 |
| `auth.*` | 见上 | 双层登录口令 |
| `server_host` | `127.0.0.1` | 载荷生成器回填地址 |
| `tcp_listener.port` | `4444` | TCP 监听端口（配合 `auto_listeners`） |
| `wss_listener.port` | `0` | `0`=关 |
| `wss_listener.cert/key` | `templates/leekshell.*` | 相对 config 目录；缺失回落内置模板 |
| `wss_listener.auto_cert` | `false` | 缺证自动自签 |
| `auto_listeners` | TCP 4444 | `[{protocol,port}]` 列表 |
| `sandbox.*` | 见注释 | 蜜罐采集 |
| `bof.compiler` | `""` | gcc 路径/自动探测 |
| `bof.plugins.*` | 见注释 | 插件目录/上限/缓存 |
| `ai.*` | 默认关 | Provider 配置见 §8 |

---

## 3. 命令行参数（exe / ELF 完全一致）

```
c2_server [参数]
```

| 参数 | 作用 | 默认 |
|---|---|---|
| `-c, --config <路径>` | 指定 config.yaml 路径 | `config.yaml`（exe/ELF 同目录） |
| `-p, --port <N>` | TCP C2 监听端口（覆盖配置） | 用配置 |
| `--web-port <N>` | Web 控制台端口（覆盖配置） | 用配置 |
| `--wss-port <N>` | WSS 监听端口，`0`=关闭（覆盖配置） | 用配置 |
| `--wss-cert <路径>` | TLS 证书路径（覆盖配置） | 用配置 |
| `--wss-key <路径>` | TLS 私钥路径（覆盖配置） | 用配置 |
| `--wss-auto-cert` | 证书缺失时自动生成自签名证书 | off |
| `--debug` | 开启结构化调试日志（stderr） | off |
| `--preauth-user/-pass <值>` | 第 1 层登录账号/口令（覆盖配置） | 用配置 |
| `--login-user/-pass <值>` | 第 2 层登录账号/口令（覆盖配置） | 用配置 |
| `--server-host <值>` | 载荷生成器默认回填地址（覆盖配置） | 用配置 |

### 常用组合示例

```bash
# 全功能：TCP 4444 + WSS 8443 + Web 4433（证书由 config 提供）
c2_server

# 只开 TCP，改 web 端口，静默关闭 WSS
c2_server --web-port 8445 --wss-port 0

# 显式指定配置 + 用命令行覆盖口令/地址（不改配置文件）
c2_server -c /etc/lks/config.yaml --login-pass 'S3cr3t' --server-host 1.2.3.4

# 开 WSS 并让它自动生成自签名证书（首次运行）
c2_server --wss-port 8443 --wss-auto-cert

# 排查用：开启详细日志（stderr）
c2_server --debug
```

---

## 4. 配置优先级

```
命令行参数  >  环境变量 LKS_*  >  config.yaml  >  内置默认值
```

- 未提供 config.yaml 时：程序自动生成默认配置（含上述默认口令/端口）。
- `server.secret_key` 留空时：首次运行生成随机 64-hex 并**写回 config.yaml**；若写不进去则本次用临时随机密钥（会话重启失效）。

---

## 5. 运行环境与文件定位

### 5.1 单文件（exe / ELF）

| 目录 | 作用 |
|---|---|
| exe/ELF **同目录** | 用户可编辑配置：`config.yaml`、`plugins/bof`(可选覆盖)、`ai_store.json`、`ai_data/`、`results/visitors` |
| 包内只读资源（运行时解压到临时目录，退出即清） | Web UI、Agent 载荷模板、默认 WSS 证书、内置示例 BOF |

模板、载荷模板、默认证书都从包内只读资源自动发现；Web 配置、`ai_store`、`results` 都写 exe 同目录。

### 5.2 源码运行

```bash
python c2_server.py -c config.yaml
```
- 无 `-c` 时默认读 **脚本所在目录** 的 `config.yaml`。
- `--debug` / config `web.debug` 打印结构化日志（stderr）。

### 5.3 运行中生成/持久化文件

| 文件/目录 | 内容 | 说明 |
|---|---|---|
| `config.yaml` | 配置 + 生成的 `secret_key` | 可改，改动即时或重启生效 |
| `ai_store.json` | AI 动态设置/记忆/知识/技能 | Web「⚙ AI 设置」页写入；覆盖 config 的 `ai.*` |
| `ai_data/` | `memory/ knowledge/ skills/` 的 md 文件 | 只读参考，可手工编辑 |
| `results/visitors/visitors.jsonl` | 沙箱采集的访客记录 | 失败口令/指纹/请求头 |

---

## 6. 环境变量（LKS_*）

部分优先级高于 config.yaml、低于 CLI：

| 变量 | 对应配置 |
|---|---|
| `LKS_WEB_PORT` | `web.port` |
| `LKS_DEBUG` | `web.debug` |
| `LKS_PREAUTH_USER` / `LKS_PREAUTH_PASS` | 第 1 层登录 |
| `LKS_LOGIN_USER` / `LKS_LOGIN_PASS` | 第 2 层登录 |
| `LKS_TCP_PORT` | `tcp_listener.port` |
| `LKS_WSS_PORT` | `wss_listener.port` |
| `LKS_SERVER_HOST` | `server_host` |
| `LKS_SANDBOX_ENABLED` / `LKS_SANDBOX_TRUST_PROXY` | 沙箱 |
| `LKS_SANDBOX_RECORD_CREDS` / `LKS_SANDBOX_CAPTURE_HEADERS` | 沙箱 |
| `LKS_SANDBOX_COLLECT_FINGERPRINT` | 沙箱 |
| `LKS_SANDBOX_ENRICH_URL` / `LKS_SANDBOX_ENRICH_TOKEN` | 沙箱情报接口 |
| `LKS_BOF_CC` | `bof.compiler` |
| `LKS_BOF_PLUGINS_ENABLED` / `LKS_BOF_PLUGIN_DIR` / `LKS_BOF_PLUGIN_MAX` | BOF 插件 |
| `ANTHROPIC_AUTH_TOKEN` / `LKS_ANTHROPIC_KEY` | Anthropic 凭据（Bearer / x-api-key） |
| `LKS_DEEPSEEK_KEY` | DeepSeek 凭据 |

Provider 凭据优先级：**环境变量 > config/store 内联值**（内联值不落日志）。

---

## 7. Web 控制台

- 地址：`http://<host>:<web.port>`（默认 `:4433`）；`web.https=true` 时为 `https://…`
- 双层登录见 §1.2；未预认证的访问记录进沙箱。
- 顶部功能区：Agent 表、命令终端、文件管理、进程/服务管理、环境侦察、Listeners、Payload Generator、凭据、**BOF**、Downloads、**AI 设置**、**AI 控制台**。

---

## 8. AI 模块（Web 实时配置）

> 所有 AI 配置也**可以在 Web「⚙ AI 设置」页实时调整**，保存即生效、跨重启持久化（写 `ai_store.json`）。

面板提供：
- **启用 AI 控制台** 复选框（等价于 `ai.enabled`）
- Provider 切换：`ollama` / `anthropic` / `deepseek`
- `Model / Base URL / API Key/Tokken / Token 环境变量 / Timeout`
- **测试连通** 按钮：用「当前表单值（无需先保存）」向模型发一条极小请求验证连通/鉴权/model，返回延迟或具体错误（服务端 `POST /api/ai/test`）
- 保存时先做 provider 可构建性预校验：坏配置返回 400 **不落盘**，避免把已启用的 AI 打挂
- 保存后立即重建 provider（`ai_settings_changed` 广播，多标签页同步刷新）

凭据映射（面板的 Token 框）：
| Provider | Token 框 → 字段 | 说明 |
|---|---|---|
| anthropic | `auth_token` | 网关 Bearer token；也可用原生 `api_key` |
| deepseek | `api_key` | OpenAI 兼容 Bearer |
| ollama | 无 | 本地无凭据 |

留空即保留旧值；Token 在接口/日志中始终脱敏。

---

## 9. 联通性自检与快速验证

```bash
# 登录后验证 socket/后端正常
curl -s http://<host>:<port>/api/auth/status
# → {"authenticated":false,"preauth":false}

# AI：查看当前合并配置（enabled/_active 为运行时态）
curl -s -b <cookie> http://<host>:<port>/api/ai/settings

# AI：用表单候选配置测试连通（POST，未保存也可）
curl -s -b <cookie> -X POST -H 'Content-Type: application/json' \
  -d '{"provider":"anthropic","anthropic":{"model":"claude-sonnet-5","base_url":"https://api.anthropic.com","auth_token":"sk-...","timeout":20}}' \
  http://<host>:<port>/api/ai/test
# → {"status":"ok","provider":"anthropic","model":"...","latency_ms":...} 或 {"status":"error","error":"..."}
```

> socket.io 控制台若出现 `400 "Not an accepted origin."`：说明有 Origin ≠ Host 的跨源客户端在连 `/socket.io`。请用与页面**同一 origin** 直连（`http://<该IP>:<port>`）或让代理保留 Host；详见排障记录。

---

## 10. 安全与运维提示

- 首次部署请改：`auth.*` 口令、`server.secret_key`（自动）、控制台访问来源。
- 打包体不含敏感配置；分发时把 `config.yaml`/证书 单独放 exe 同目录即可。
- WSS 默认证书为包内只读示例，用于开箱即用；生产请用 `--wss-cert/--wss-key` 提供自己的证书或 `--wss-auto-cert` 自签。
- 每 agent AI 会话仅存内存、不落盘，避免 AI 建议命令成为新情报泄露面。
- Linux 常驻推荐：`setsid nohup ./c2_server > server.log 2>&1 < /dev/null &`（或 systemd unit）。
- 更新二进制：先备份旧 `c2_server`，替换后原 config.yaml 继续沿用（secret_key 不变则会话可续）。
